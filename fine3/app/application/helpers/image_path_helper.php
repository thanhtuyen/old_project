<?php  if ( ! defined('BASEPATH')) exit('No direct script access allowed');
/*
function getImageFromS3($targetId, $imageName = null, $position)
{
    return '';
    // $CI = & get_instance();
    // $CI->config->load('s3', true);
    // $bucket = $CI->config->item('bucket', 's3');

    // $imageName = urlencode($imageName);

    // return $CI->config->item('s3_website', 's3') . $bucket . "/upload/$position/$targetId/$imageName";
}
 */